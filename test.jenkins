#!groovy

pipeline {
  agent any

  stages {
    stage('Установка зависимостей') {
      steps {
        sh 'npm install'
      }
    }
    stage('Build Project') {
      steps {
        sh 'npm run build'
      }
    }
  }
}